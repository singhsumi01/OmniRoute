- **Sonar quality gate**: feed the coverage lcov to the SonarQube scan at the path it expects (`coverage/lcov.info` — new-code coverage no longer reads 0%), await the async `isCloudEnabled()` gate in the Kiro auto-import route (cloud sync ran even when disabled), fix the dead `structuredClone` fallback in the reasoning-split clone, handle the async `ReadableStreamDefaultReader.cancel()` rejection in the codex executor, use `localeCompare` for deterministic sorts, confine `classify-pr-changes.mjs` to the workspace (path-traversal guard), and rebuild better-sqlite3 in Docker with npm's bundled node-gyp instead of `npx --yes`
